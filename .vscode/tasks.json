{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "BUILD_SUCH_OS",
            "type": "shell",
            "command": "clang++ SUCH_OS_LIB/*.cpp Emulator/*.cpp -lraylib -lGL -lm -lpthread -ldl -lrt -lX11 -lncurses -g -o SUCH_OS_EMULATOR.runme",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}